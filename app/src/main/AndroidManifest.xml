<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Libro">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CabinetBooksActivity"
            android:label="Книги на полке"
            android:theme="@style/Theme.Libro.NoActionBar" />
        <activity
            android:name=".BookDetailsActivity"
            android:label="Детали книги"
            android:theme="@style/Theme.Libro.NoActionBar" />
        <activity
            android:name=".AddBookActivity"
            android:label="Добавить книгу"
            android:theme="@style/Theme.Libro.NoActionBar" />
        <activity
            android:name=".AddShelfActivity"
            android:label="Новый шкаф"
            android:theme="@style/Theme.Libro.NoActionBar" />
        <activity
            android:name=".EditBookActivity"
            android:label="Изменить книгу"
            android:theme="@style/Theme.Libro.NoActionBar" />
        <activity
            android:name=".EditShelfActivity"
            android:label="Изменить полку"
            android:theme="@style/Theme.Libro.NoActionBar" />
    </application>

</manifest>